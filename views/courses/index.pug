extends ../layouts/main.pug

block content
  .row
    .col-12
      .d-flex.justify-content-between.align-items-center.mb-4
        h1 📚 Course List
        a.btn.btn-success(href=routes.newCoursePath()) ➕ Add Course
      
      if courses.length === 0
        .alert.alert-info
          | No courses yet. 
          a(href=routes.newCoursePath()) Add the first one!
      else
        .row
          each course in courses
            .col-md-6.mb-3
              .card
                .card-body
                  h5.card-title 📖 #{course.title}
                  p.card-text #{course.description}
                  p.card-text
                    small.text-muted ID: #{course.id}
                  
                  .d-flex.gap-2.mt-3
                    a.btn.btn-primary.btn-sm(href=routes.coursePath(course.id)) View
                    a.btn.btn-warning.btn-sm(href=routes.editCoursePath(course.id)) Edit
                    form.d-inline(action=routes.coursePath(course.id), method='post')
                      input(type='hidden', name='_method', value='DELETE')
                      button.btn.btn-danger.btn-sm(type='submit', onclick="return confirm('Are you sure you want to delete this course?')") Delete