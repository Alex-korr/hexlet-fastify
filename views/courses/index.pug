extends ../layouts/main.pug

block content
  .row
    .col-12
      .d-flex.justify-content-between.align-items-center.mb-4
        h1 📚 Course List
        a.btn.btn-success(href='/courses/new') ➕ Add Course
      
      if courses.length === 0
        .alert.alert-info
          | No courses yet. 
          a(href='/courses/new') Add the first one!
      else
        .row
          each course in courses
            .col-md-6.mb-3
              .card
                .card-body
                  h5.card-title 📖 #{course.title}
                  p.card-text #{course.description}
                  p.card-text
                    small.text-muted ID: #{course.id}